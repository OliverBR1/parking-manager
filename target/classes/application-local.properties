# ---- DATABASE LOCAL (fora do Docker) ----
spring.datasource.url=jdbc:mysql://localhost:3306/parkingdb?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=admin123

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Porta da aplica��o
server.port=3003

# Simulador rodando no host
simulator.base-url=http://localhost:3000
